version: '1'
components:
  - name: web
    source: os/apps/web
    type: nodejs
    env:
      NODE_ENV: production
    build:
      - npm install
      - npm run build
    start:
      - npm start
    port: 8080
